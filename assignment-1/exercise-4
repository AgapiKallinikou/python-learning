n = int(input('How many asterisks on the diagonal? '))
print(' '*(n-1) + '*')   # The first line
for i in range(2,n):
    # Line i (2<=i<n) is formed by:
    # (n-i) spaces - asterisk - (i-2) spaces - asterisk
    print(' '*(n-i) + '*' + ' '*(i-2) + '*')
print('*'*n)           # The last line
